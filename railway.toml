# Railway: WSGI module is config.wsgi (file config/wsgi.py), callable is "application".
# Bind to 8080 so health check passes. PYTHONPATH=. so config is importable from repo root.
[deploy]
startCommand = "PYTHONPATH=. gunicorn config.wsgi:application --bind 0.0.0.0:8080 --workers 1 --timeout 120"
